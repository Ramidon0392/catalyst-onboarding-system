"""\nField validators for the Catalyst Onboarding System.\n\nValidates intake form fields before CRM record creation.\n"""\n\nimport re\n\n\ndef validate_email(value: str) -> bool:\n    """\n    Validate that a string matches the email format: local@domain.tld\n    """\n    if not value:\n        return False\n    at_count = value.count("@")\n    if at_count != 1:\n        return False\n    local, domain = value.split("@", 1)\n    if not local:\n        return False\n    if not domain or "." not in domain:\n        return False\n    return True\n\n\n_PHONE_PATTERN = re.compile(\n    r"^\d{10}$"\n    r"|^\d{3}-\d{3}-\d{4}$"\n    r"|^\(\d{3}\) \d{3}-\d{4}$"\n)\n\n\ndef validate_phone(value: str) -> bool:\n    """\n    Validate that a string matches a 10-digit North American phone format.\n    Empty string is accepted (phone is optional).\n    """\n    if value == "":\n        return True\n    return bool(_PHONE_PATTERN.match(value))\n